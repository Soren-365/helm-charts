apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avantweb.fullname" . }}-test-connection"
  labels:
{{ include "avantweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "avantweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
