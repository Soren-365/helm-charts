apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "avant-test.fullname" . }}-test-connection"
  labels:
{{ include "avant-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "avant-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
