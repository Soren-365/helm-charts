apiVersion: v1
kind: Service
metadata:
  name: {{ include "avantweb.fullname" . }}
  labels:
    {{- include "avantweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "avantweb.selectorLabels" . | nindent 4 }}
