apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeHooks.fullname" . }}-test-connection"
  labels:
{{ include "kubeHooks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kubeHooks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
