apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeHooks.fullname" . }}
  labels:
    {{- include "kubeHooks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubeHooks.selectorLabels" . | nindent 4 }}
