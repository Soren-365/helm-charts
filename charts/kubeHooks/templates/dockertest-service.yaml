apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -c
    kompose.version: 1.19.0 (f63a961c)

  labels:
    io.kompose.service: kubeHooks
  name: kubeHooks
spec:
  ports:
  - name: "5000"
    port: 5000
    targetPort: 5000
  selector:
    io.kompose.service: kubeHooks
status:
  loadBalancer: {}
